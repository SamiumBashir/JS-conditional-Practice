const phones = ['apple', 'samsung', 'One plus', 'Infinity'];
const names = ('apple', 'samsung', 'oneplus', 'vivo');

if (Array.isArray (names)){
    console.log("lets buy a phone")
}

else{
    console.log("onno dokan dekhi")
}